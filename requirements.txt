# Python 3.14 Note: Install with --only-binary=:all: flag on Windows
# Example: pip install --only-binary=:all: -r requirements.txt
# If pandas installation fails, install pyarrow first: pip install pyarrow==23.0.0

streamlit>=1.28
anthropic>=0.18
psycopg2-binary>=2.9
python-dotenv>=1.0
pandas>=1.4.0
sqlalchemy>=2.0
